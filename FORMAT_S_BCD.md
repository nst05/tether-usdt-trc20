# Поле S — формат BCD

В дампе живут **два разных формата** хранения значений. Раньше поддерживался
только один.

| | Поле T | Поле S |
|---|---|---|
| Кодирование | value × 100, BIG-ENDIAN | упакованный BCD, обратный порядок байт |
| Размер записи | 42 байта | 65 байт (0x41) |
| Копий значения | 2 | **4** |
| CRC | CRC-16 CCITT, 2 байта | **нет** |
| Целостность | по CRC | по совпадению копий |
| Пример | 9442.36 @ 0x01C1 | **9399.56 @ 0x1446** |

## Как найдено 9399.56

Прямого поиска не хватило: в fixed-point это `00 0E 57 B4`, и такой
последовательности в дампе нет. Проверены и отброшены все масштабы
(×1, ×10, ×100, ×1000, ×10000), длины 2/3/4/6/8 байт, оба порядка байт,
float32/float64, текст и обычный BCD.

Зацепка нашлась при разборе области 0x11C0 и дальше: там лежали значения,
похожие на наш диапазон, но в непривычной записи. Оказалось — BCD задом
наперёд:

```
9399.56  ×100  →  939956  →  цифры "00939956"  →  байты 00 93 99 56
                                     обратный порядок  →  56 99 93 00
```

Байты `56 99 93 00` в дампе есть — и не один раз.

## Структура записи S

Запись 65 байт, значение продублировано четыре раза:

```
+00  3 байта    заголовок          01 01 44
+03  4 байта    значение (BCD)     56 99 93 00   ← копия 1
+07  4 байта    копия              56 99 93 00   ← копия 2
+11  12 байт    нули               00 × 12
+23  4 байта    копия              56 99 93 00   ← копия 3
+27  4 байта    копия              56 99 93 00   ← копия 4
+31  34 байта   нули до конца записи
```

Запись 9399.56 по 0x1446, копии по адресам **0x1449, 0x144D, 0x145D, 0x1461**.

## Про CRC у поля S

CRC у этого формата **нет** — это проверено, а не предположено:

1. Хвост записи (байты +31…+64) — сплошные нули. Класть CRC некуда.
2. Подозрительные пары байт рядом с записями (`0D 3E` по 0x1556, `13 18`
   и `08 19` в логе по 0x2F40) прогнаны через CRC-16 в шести вариантах
   (CCITT-FALSE, XMODEM, KERMIT, MODBUS, ARC, CCITT-1D0F), по всем
   диапазонам длиной до 160 байт, в обоих порядках байт — **ни одного
   совпадения**.
3. CRC самой записи, посчитанный всеми шестью вариантами по пяти разным
   диапазонам, в окрестностях записи (±200/300 байт) **не встречается**.

Вывод: целостность поля S держится на четырёх копиях. Поэтому правка
обязана обновлять **все четыре** — иначе прошивка увидит расхождение.

Для сравнения, у поля T CRC есть и сходится: блок по 0x01C1 даёт
CRC 0xAC55, и ровно это в дампе и записано.

## Все записи формата S в дампе

| Смещение | Значение | Смещение | Значение |
|---|---|---|---|
| 0x11BC | 9066.64 | 0x1383 | 9308.45 |
| 0x11FD | 9126.74 | 0x13C4 | 9320.94 |
| 0x123E | 9127.35 | 0x1405 | 9328.87 |
| 0x127F | 9179.60 | **0x1446** | **9399.56** |
| 0x12C0 | 9226.71 | 0x14C8 | 9008.40 |
| 0x1301 | 9263.06 | 0x1509 | 9054.48 |
| 0x1342 | 9288.82 | | |

Шаг между записями — 0x41 (65 байт), в одном месте пропуск в две позиции.

## Как редактировать

Вкладка «I2C/EEPROM», у каждого слота свой выпадающий список формата:

- **T — fixed-point + CRC** — HEX и CRC-16 считаются на лету, пишется
  42-байтовый блок, CRC пересчитывается автоматически.
- **S — BCD ×4 копии** — HEX показывает четыре BCD-байта, поле CRC
  показывает `4 × (нет CRC)`, пишутся 16 байт: по 4 байта в каждую копию.

Кнопка **«Найти в файле»** разбирает открытый дамп и создаёт слот на каждую
найденную запись обоих форматов. **«Найти записи»** делает то же самое,
вычитав микросхему целиком.

Слоты независимы: правка одного не трогает остальные. Для S это проверено
тестом — за пределами четырёх копий не меняется ни один байт, включая
заголовок, нули между копиями и соседние записи.

## Диапазон значений

Поле S: `0.00 – 999999.99` (8 BCD-цифр).
Поле T: `0.00 – 42949672.95` (32-битное целое).
