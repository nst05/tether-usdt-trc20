# 208_CE V8530P · MSP4xx — редактор памяти

Версия редактора: **3.0.2** — две независимые группы тарифов, раздельные режимы BIN/CH341 и обязательная проверка профиля процессора V8530/MSP4xx перед записью.

Это однообразный редактор полного SPI-дампа `внешняя SPI` размером `0x80000` байт, построенный по дизассемблированной прошивке `ver 10.14, Oct 29 2022`.

## Профили процессоров: V8530, MSP4xx, STM32

Раскладка данных у всех трёх одинаковая (время @0x0000, четыре банка `0x44` от @0x0020,
13 ячеек `u40le`, T1…T8 в ячейках 3…10, пары направлений 0/2 и 1/3), различаются
контрольная сумма записи и сдвиг резервной копии:

| Профиль | Контрольная сумма записи | Сдвиг резерва |
|---|---|---|
| **V8530** | младшие 16 бит CRC-32 MSB-first, `poly=0x04C11DB7`, `init=0xFFFFFFFF`, без финального XOR | `0x19E0` |
| **MSP4xx** | CRC-16 CCITT, `poly=0x1021`, обратный порядок бит на входе, `init=0x68D3`, без XOR на выходе | `0x19E0` |
| **STM32** | тело обрабатывается 16-битными словами little-endian: слово складывается по модулю 2 с младшими битами регистра, затем 32 сдвига с `poly=0x04C11DB7`; `init=0xFFFFFFFF`, младшие 16 бит | `0x1B98` |

Сумма во всех профилях хранится в последних двух байтах записи little-endian.

Профиль выбирается списком **ПРОЦЕССОР** в верхней панели: **Авто · V8530 · MSP4xx · STM32**.
«Авто» проверяет все три схемы на энергетических банках, статических записях и их зеркалах;
побеждает та, по которой сходится больше. Запись блокируется, если выбранный процессор
противоречит дампу или профиль не определяется однозначно.

Ярлыки запуска: `run_editor.bat` (Авто), `run_editor_v8530.bat`, `run_editor_msp432.bat`,
`run_editor_stm32.bat`; из командной строки — `editor.py --crc=stm32`.

## Один физический SPI, два логических пути

Прошивка использует два набора функций доступа, но редактор отображает их на один BIN:

- `small-path` — нижние `0x2000` байт того же SPI: текущее время, T1…T8, оперативные настройки и близкие резервные копии;
- `large-path` — прямые адреса полного SPI: энергетические архивы, события, крупные конфигурации и удалённые резервные записи.

Никакой второй файл NVM не требуется. Изменения по адресам `small:*` записываются в те же смещения полного `внешняя SPI.bin`.

## Уже воспроизведено точно

- CRC записи: MSB-first CRC-32 `poly=0x04C11DB7`, `init=0xFFFFFFFF`, без final XOR; в записи сохраняются младшие 16 бит little-endian;
- чтение основной копии и переход к резервной при неверной CRC;
- 10-байтная запись даты/времени: основная `small:0x0000`, резерв `small:0x19E0`;
- четыре текущих энергетических банка: `small:0x0020 + bank*0x44`, резерв `small:0x1A00 + bank*0x44`;
- активный тариф `1..8` в младшем полубайте `tariff_control`, с сохранением остальных флагов;
- два блока временных счётчиков: восемь пар `timestamp от 2000-01-01 + uint32 counter` в каждом;
- энергетическая запись `0x44` байта: `13 × u40le`, байт поколения/маркера, CRC16;
- T1…T8 — ячейки 3…10; «Сумма» вычисляется прошивкой как сумма восьми тарифов;
- оператор вводит обычными десятичными числами все T1…T8 первой группы и, по отдельному чекбоксу, T1…T8 второй группы;
- штатное соответствие банков сохранено: `0/2 = 1.8.x/3.8.x` и `1/3 = 2.8.x/4.8.x`;
- реактивные банки рассчитываются попарно: `3.8 = 1.8 × K`, `4.8 = 2.8 × K`;
- основная кнопка синхронно обновляет current primary/backup и все архивные слоты типов `0/1/2/5` только для включённых пар;
- кольцевые архивы AT25 типов 0/1/2/5, их размеры, modulo индекса, четыре банка на слот; после активной части типа 0 сохранена штатная резервная зона `0x4400` байт;
- все 70 кольцевых журналов событий AT25 `0x47E70..0x49DEF`: время в секундах от 2000-01-01, общий sequence, status, дополнительное значение, индивидуальные счётчики и modulo слота;
- безопасное сохранение одного нового BIN, JSON-аудит и план стирания/программирования AT25 по секторам и страницам;
- браузер всех уже найденных статических записей с raw-редактированием и автоматическим обновлением обеих копий и CRC.

## Выбор процессора: V8530 или MSP4xx

Приборы этой серии встречаются с двумя процессорами. Раскладка памяти у них одинаковая
(время @0x0000 и резерв @0x19E0, четыре банка `0x44` от @0x0020, ячейки `u40le`), а вот
контрольная сумма записи считается по-разному:

| Процессор | Алгоритм контрольной суммы | Где встречается |
|---|---|---|
| **V8530** | младшие 16 бит CRC-32 MSB-first, `poly=0x04C11DB7`, `init=0xFFFFFFFF`, без финального XOR (функция прошивки `0x3B8EC`) | дампы `внешняя SPI` и `24C64` прошивки `10.14` |
| **MSP4xx** | CRC-16 CCITT, `poly=0x1021`, обратный порядок бит на входе, `init=0x68D3`, без XOR на выходе | дампы вида `24C64_MKMSP430.bin`; точная модель CPU по одному EEPROM-дампу не определяется |

Процессор выбирается выпадающим списком **ПРОЦЕССОР** в верхней панели, рядом с кнопками
работы с файлом: **Авто · V8530 · MSP4xx**. Действующая схема показана в правой панели
(строка «Контроль»).

- **Авто** (по умолчанию) — обе схемы проверяются на всех статических записях, побеждает та,
  по которой сходится больше. Если не сходится ни одна (пустой или чужой образ), берётся V8530.
- **V8530** или **MSP4xx** — схема закрепляется жёстко, автоподбор не применяется. Это нужно,
  когда дамп повреждён настолько, что подбирать не по чему.

Переключение действует сразу на открытом образе: записи перечитываются, счётчик «CRC записей»
и карта секторов обновляются. Выбранная схема используется и при записи, поэтому изменённый
дамп остаётся пригодным для того же прибора.

Проверка на предоставленных 8-КБ дампах показала: адреса, зеркала, размер записи `0x44`,
`13 × u40le` и нумерация четырёх банков у двух вариантов совпадают. Отличается именно CRC:
в дампе V8530 проходят 46 служебных и 8 энергетических копий только по схеме V8530, а в
`24C64_MKMSP430.bin` те же 54 записи проходят только по профилю MSP4xx; перекрёстных
совпадений нет.

Перед любой записью действует обязательный processor preflight. В режиме «Авто» запись
разрешается только при единственном уверенно определённом профиле. При пустом/сильно
повреждённом дампе нужно вручную выбрать процессор. Если выбран процессор, противоречащий
валидным записям дампа, запись блокируется.

Тот же выбор доступен из командной строки и вынесен в отдельные ярлыки запуска:

```powershell
run_editor.bat                  # Авто
run_editor_v8530.bat            # только V8530
run_editor_msp432.bat           # профиль MSP4xx (имя файла оставлено для совместимости)
editor.py --crc=msp432 C:\ce208\dump.bin
```

Текущая схема видна в правой панели («Процессор» и «Контроль»), в нижней строке состояния
и в сообщении при открытии файла.

## Сборка .exe

В комплекте есть готовая конфигурация PyInstaller: значок, отсутствие консольного окна
и упаковка в один файл.

Проще всего — двойной щелчок по **`build_exe.bat`**: он поставит PyInstaller, очистит
прошлую сборку и соберёт `dist\CE208_SPI_Editor_v3.exe`.

Вручную то же самое:

```powershell
python -m pip install --upgrade pyinstaller
python -m PyInstaller --noconfirm ce208_editor.spec
```

Что задано в `ce208_editor.spec`:

- `console=False` — окно программы без чёрной консоли;
- `icon="assets/ce208.ico"` — значок самого `.exe` (тот же, что у окна);
- `datas` — `assets/ce208.ico` и `assets/ce208.png` кладутся внутрь сборки, поэтому значок
  окна работает и в собранном виде (`app_icon.py` берёт путь из `sys._MEIPASS`);
- `upx=False` — сжатие UPX отключено: оно часто вызывает ложные срабатывания антивирусов;
- лишние библиотеки (numpy, pandas, PIL и прочее) исключены — файл получается компактнее.

Важные замечания:

- собирать нужно **на Windows** тем же Python 3, которым программа запускается, — PyInstaller
  не делает кросс-сборку: на Linux получится Linux-бинарь, на Windows — `.exe`;
- Python обязательно с Tkinter (обычный установщик с python.org подходит);
- если нужна прямая запись через CH341, установите `i2cpy` **до** сборки
  (`python -m pip install i2cpy`) — тогда она попадёт внутрь `.exe`; без неё программа
  работает с файлами образов, а вкладка «Прямая запись» сообщит, что библиотеки нет;
- собранный `.exe` самодостаточен: рядом с ним ничего класть не нужно, дампы открываются
  через «Открыть дамп…» или перетаскиванием пути в аргументах ярлыка;
- жёсткий режим процессора задаётся ключом и в собранном виде:
  `CE208_SPI_Editor_v3.exe --crc=msp432`.

Сборка проверена: `pyinstaller --noconfirm ce208_editor.spec` отрабатывает без ошибок,
полученный файл запускается, показывает экран загрузки и строит окно со значком.

## Оформление

Логика работы с памятью не изменялась — переработан только внешний вид.

- **Иконка.** Знак программы (стальная пластина, корпус микросхемы с выводами и импульс энергии)
  рисуется кодом в `app_icon.py`; готовые `assets/ce208.ico` (16…128 px) и `assets/ce208.png`
  лежат в комплекте. Пересоздать: `python app_icon.py`. Если файлы удалить, иконка соберётся
  в памяти при запуске — окно никогда не остаётся без значка.
- **Экран загрузки** (`splash.py`). Тёмная приборная заставка: знак, версия, журнал инициализации
  с отметками выполненных стадий, сводка параметров восстановленной модели, вращающийся индикатор
  занятости и полоса выполнения с процентами. Кадры анимации прокручиваются вручную, поэтому
  движение видно и во время тяжёлых стадий, когда цикл событий Tk ещё не запущен.
- **Палитра «инженерная сталь»** (`ui_theme.py`). Средние неяркие тона: тёмно-стальная шапка и
  статусная строка, светло-серый рабочий фон, приборный синий акцент и три сигнальных цвета —
  норма, внимание, ошибка. Все стили ttk (кнопки, поля, вкладки, таблицы, полосы прокрутки)
  настраиваются в одном месте.
- **Индикаторы выполнения.** `AnimatedProgressbar` доезжает до нового значения плавно, а
  `ProgressPanel` показывает над полосой подпись стадии и процент. Цвет полосы отражает исход:
  синий — работа, зелёный — успех, красный — сбой. Полосы стоят на вкладках «Энергия» и
  «Прямая запись», а также в правой панели (контроль CRC и объём предстоящей прошивки).
- **Шапка.** Знак, название, версия, часы ПК, диаграмма активности и три индикатора состояния:
  загруженный образ, доля записей с верной CRC, наличие программатора CH341.
- **Правая панель «Состояние образа».** Файл, тип и объём памяти, число изменённых байт,
  время прибора, активный тариф, счётчик событий, профиль программатора; полосы «CRC записей»
  и «Секторы к прошивке»; карта 128 секторов SPI по 4 КиБ — серый сектор не тронут, синий
  требует стирания и записи. Все значения читаются из модели и обновляются после каждой операции.
- **Статусная строка.** Светодиод состояния (готов / работа / ошибка), текст последней операции,
  бегущая полоса занятости и техническая сводка: изменённые байты, CRC, объём, счётчик событий.

Дополнительных зависимостей оформление не требует: только стандартный Tkinter.

## Запуск

Запустите `run_editor.bat` двойным щелчком. Он использует доступный Python 3 с Tkinter; на этой машине системный `py -3` зарегистрирован в Windows, но указывает на удалённый пакет WindowsApps, поэтому запуск через BAT надёжнее.

Можно сразу передать полный образ первым аргументом:

```powershell
run_editor.bat C:\ce208\внешняя SPI.bin
```

## Масштаб показаний

Прошивка сначала вычисляет `quotient = floor(raw / K)`, затем вставляет десятичную точку. `K` находится в рабочей структуре RAM `0x2000ABD4 + 0x14` и формируется из конфигурационных записей SPI. Пока конкретное поле конфигурации `K` не подписано окончательно, оператор задаёт `K` и число десятичных знаков явно. Для записи программа выбирает минимальный `raw`, который штатная функция покажет точно.

## Синхронная запись двух групп показаний

На вкладке «Текущая энергия»:

1. введите T1…T8 первой группы `1.8.x`;
2. если нужно изменить второе направление, установите чекбокс **«Редактировать вторую группу тарифов (2.8.x / 4.8.x)»** и введите её собственные T1…T8;
3. оставьте включённой авто-реактивную и задайте коэффициент K, если требуется попарный расчёт `3.8` и `4.8`;
4. выберите общий действующий тариф `1..8`;
5. нажмите **«СОГЛАСОВАТЬ И ЗАПИСАТЬ»**.

При снятом чекбоксе банки `1/3` (`2.8/4.8`) и их архивы не изменяются. При установленном чекбоксе программа записывает отдельные значения второй группы. Для каждой включённой пары вычисляются «Сумма», current primary/backup и CRC; в полном SPI также переписываются соответствующие архивные банки. Для уже валидных архивов сохраняются маркер и дополнительные энергетические ячейки; для пустых используется указанный оператором маркер.

Действующий тариф Tn остаётся одним общим штатным параметром прибора. Чекбокс второй группы управляет только её накопителями и не создаёт отдельный календарь тарифов.

Два режима записи не объединяются:

- при работе с полным BIN `внешняя SPI` синхронизируются выбранные пары текущих записей и их архивы SPI;
- на вкладке прямой записи CH341 изменяются только текущие primary/backup в 8-КБ памяти. Архивы `внешняя SPI` и журнал событий в этом режиме не затрагиваются, даже если ранее был открыт полный SPI-образ.

Архивные снимки после такой операции содержат введённые значения во всех периодах. Это намеренная синхронизация, а не восстановление реальной исторической динамики.

## Важное ограничение предоставленного файла

В `C:\ce208\внешняя SPI.bin` находятся пять одинаковых тестовых 128-байтных блоков, а текущие записи в нижней области и энергетические архивы `0x03A38..0x15097` ещё не сформированы. Это согласуется с SPI, который не успел накопить рабочую базу. Для получения фактических T1/T2 и времени нужен полный SPI-дамп после нормальной работы прибора — не отдельная NVM.

## Проверка модели

```powershell
C:\Users\ixd\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m unittest -v test_ce208_model.py
```

Тесты проверяют контрольную сумму заводского тестового блока (`0x12D0`), зеркальные записи времени, активного тарифа и текущей энергии, независимость пар `0/2` и `1/3`, попарный расчёт реактивной энергии, неизменность отключённой второй группы, карту архивов, все границы 70 журналов, упаковку времени события, кольцевое перезаписывание и точную арифметику отображения.

## Что не является записываемым значением

Напряжение, ток, мощность, частота, температура и состояния входов/выходов на части дисплейных экранов вычисляются из текущего метрологического RAM и периферии. В BIN они не хранятся, поэтому редактор не выдаёт их за параметры внешней памяти. Карта всех 49 экранов и LCD-кодек находятся рядом с отчётом дизассемблирования.
