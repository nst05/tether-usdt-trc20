# CE208 — Интеграция протокола R1/W1 и структуры памяти

## I. Карта памяти: от оптопорта до SPI

### A. Три уровня хранения данных

```
Пользователь (оптопорт R1/W1)
    ↓
Протокол IEC 62056-21 (адреса 0x0000–0xFFFF)
    ↓
Парсер @ 0x326D2 (маршрутизация)
    ↓
    ├─ RAM структуры (0x2000xxxx) — текущее состояние
    │  ├─ RTC @ 0x2000C734 (10 байт: HH MM SS DOW DD MM YY FLAGS)
    │  ├─ Текущая энергия @ 0x2000C6D8 (4 банка × 0x44 = 272 байта)
    │  ├─ Текущие V/I/P (от АЦП + вычисления)
    │  └─ Тарифный календарь + флаги
    │
    └─ SPI 25DF041B (512 KB) — архив и конфигурация
       ├─ small: 0x0000–0x1FFF (8 KB быстрого доступа)
       │  ├─ Часы (основная + резервная) @ 0x0000 / 0x19E0
       │  ├─ Управление тарифом @ 0x000A / 0x19EA
       │  ├─ Текущая энергия (4 банка) @ 0x0020–0x006F / 0x1A00–0x1A6F
       │  ├─ События (счётчики) @ 0x0308–0x0337 / 0x1CE8–0x1D17
       │  ├─ Временные счётчики @ 0x0EE0–0x0FBF (2 блока)
       │  └─ Конфигурация (40+ записей)
       │
       └─ large: 0x2000–0x7FFFF (500 KB архивов)
          ├─ Архивы энергии (type 0/1/2/5) @ 0x03A38–0x15097
          ├─ 70 кольцевых журналов событий @ 0x47E70–0x49DEF
          └─ Резервные копии конфигурации
```

## II. Маршрут R1 команды (чтение параметра)

### A. Пример: R1 0x0040 (чтение текущих часов)

```
1. Пользователь → R1 SOH 0040 () ETX BCC
2. Парсер @ 0x326D2 ищет адрес 0x0040 в таблице @ 0x32800
3. Совпадение: параметр = clock_snapshot
   ├─ путь в памяти: SPI small @ 0x0000 (основная)
   ├─ размер: 0x0A (10 байт)
   ├─ кодирование: BCD (HH MM SS DOW DD MM YY FLAGS)
   └─ доступ: всегда открыто (R1)
4. Обработчик 0x30200 читает из SPI @ 0x0000:
   ├─ проверяет CRC резервной копии @ 0x19E0
   ├─ если CRC ошибка → берёт резервную
   └─ формирует ответ с BCD-кодированием
5. Счётчик → R1 SOH 0040 (24082312123045) ETX BCC
              ↑           ↑ 2024-08-23 12:30:45 (BCD)
```

### B. Пример: R1 0x0000 (чтение активной энергии по всем тарифам)

```
1. R1 SOH 0000 () ETX BCC
2. Парсер → параметр = E_A1 (сумма энергии)
   ├─ путь: SPI small @ 0x0020 (основная энергетическая запись)
   ├─ размер: 0x44 (68 байт)
   ├─ структура: 13 × u40le (5-byte little-endian значения)
   │  ├─ cell[0]: сумма всех тарифов (вычисляется как sum(T1..T8))
   │  ├─ cell[1..8]: T1..T8 (энергия по тарифам)
   │  ├─ cell[9..12]: резерв или доп. параметры
   │  └─ markers: маркер поколения + CRC-16
3. Обработчик читает банк 0 (PRIMARY):
   ├─ адрес 0x0020 + (bank * 0x44) = 0x0020
   ├─ верифицирует CRC-16 (последние 2 байта)
   ├─ если ошибка → читает резервную копию @ 0x1A00
   └─ кодирует значение в hex LE
4. Ответ содержит 13 5-byte значений в LE порядке
```

## III. Маршрут W1 команды (запись параметра)

### A. Пример: W1 0x0040 (установка времени)

```
1. Программист (с P2 активным) → W1 SOH 0040 (24082312123045) ETX BCC
2. Парсер @ 0x326D2:
   ├─ адрес 0x0040 → параметр = clock_snapshot
   ├─ проверка доступа @ 0x35600:
   │  ├─ требуется P2 для записи времени → OK
   │  └─ если нет P2 → ошибка 0x20 (ACCESS DENIED)
3. Парсер декодирует BCD (24 08 23 12 30 45 ...)
4. Обработчик @ 0x30200 копирует во временный буфер
5. NVM запись @ 0x3567C:
   ├─ вызов 0x11DC (аппаратная запись с ключами)
   ├─ разблокировка SPI: ключ 0x5AA55AA5
   ├─ запись основной копии @ 0x0000 (10 байт)
   ├─ запись резервной копии @ 0x19E0 (10 байт)
   └─ блокировка: ключ 0xA55AA55A
6. Счётчик → W1 SOH 0040 (OK) ETX BCC
```

### B. Пример: W1 0x0020 (синхронная запись энергии во все архивы)

```
1. W1 SOH 0020 (<13×5-byte LE значения>) ETX BCC
2. Парсер:
   ├─ адрес 0x0020 → параметр = CURRENT_ENERGY_PRIMARY
   ├─ требуется P2 → проверка доступа
   └─ декодирование: распарсить 13 × u40le
3. Обработчик:
   ├─ обновляет current_energy @ 0x0020 (основная)
   ├─ обновляет backup @ 0x1A00 (резервная)
   ├─ пересчитывает cell[0] = sum(cell[1..8])
   ├─ вычисляет CRC-16 для обеих копий
   └─ записывает в SPI (через 0x11DC с аппаратными ключами)
4. Синхронная операция редактора дополнительно:
   ├─ переписывает ВСЕ 198 архивных слотов (type 0/1/2/5)
   ├─ обновляет каждый слот с новыми значениями + CRC
   └─ сохраняет маркер поколения для отслеживания истории
```

## IV. Двойное хранилище (primary/backup)

### A. Логика выбора при чтении

```python
def read_parameter(address):
  # Читать основную копию
  primary = read_spi(address)
  primary_valid = verify_crc(primary)
  
  if primary_valid:
    return primary
  
  # Если основная повреждена, пробовать резервную
  if backup_address:
    backup = read_spi(backup_address)
    backup_valid = verify_crc(backup)
    if backup_valid:
      return backup
  
  # Обе повреждены - вернуть значение по умолчанию
  return default_value
```

### B. Расположение backup копий

| Параметр | Primary | Backup | Offset |
|---|---|---|---|
| Часы (clock) | 0x0000 | 0x19E0 | +0x19E0 |
| Управление (tariff_control) | 0x000A | 0x19EA | +0x19E0 |
| Текущая энергия | 0x0020 | 0x1A00 | +0x19E0 |
| События (event_sequence) | 0x0308 | 0x1CE8 | +0x19E0 |

**Паттерн**: резервная копия находится на фиксированном смещении +0x19E0 (6624 байта)

## V. Архивы энергии в SPI

### A. Четыре типа архивов

```
Archive Type 0 (основной):
  ├─ Базовый адрес: 0x03A38
  ├─ Размер слота: 0x44 (68 байт)
  ├─ Количество слотов: 128 (0x0080)
  ├─ Полный размер: 128 × 68 = 0x2200 байт
  ├─ Структура слота: как CURRENT_ENERGY (13 ячеек + маркер + CRC)
  ├─ Четыре банка на слот (T1-T8, T9-T16, T17-T24, T25-T32)
  ├─ Резервная зона: 0x4400 байт после активной части
  └─ Индексирование: по дате/времени + счётчик вызовов

Archive Type 1, 2, 5:
  ├─ Type 1: 0x10638, 40 слотов
  ├─ Type 2: 0x130B8, 10 слотов
  └─ Type 5: 0x13B58, 20 слотов
  └─ Назначение: вспомогательные выборки (почасовые, суточные, и т.д.)
```

### B. Кольцевая буферизация

```
При записи нового снимка:
1. Вычислить индекс слота (модуль количества слотов)
2. Проверить валидность текущего слота
3. Если валидный - перейти к следующему (круговой поиск)
4. Выписать новый слот с текущей энергией
5. Обновить маркер поколения (для отслеживания порядка)
6. Вычислить и сохранить CRC
```

## VI. События (Event Logs)

### A. 70 журналов событий

```
Каждый журнал определяется:
  ├─ event_id: 1-36, 101-134 (70 уникальных)
  ├─ display_code: код отображения на приборе (1..12)
  ├─ body_length: размер тела события (4..20 байт)
  ├─ capacity: количество слотов в журнале
  ├─ base: начальный адрес в SPI (0x47E70..0x49DEF)
  └─ record_length: body_length + 2 (для CRC-16)
```

### B. Структура записи события

```
[timestamp uint32] [status byte] [value] [aux fields] [CRC-16]
↑                  ↑            ↑                      ↑
4 байта            1 байт       4-18 байт              2 байта
(сек. с 2000-01-01) статус      доп. данные

Таблица @ 0x6F64C содержит точные параметры каждого журнала
```

## VII. Конфигурационные записи (FIXED_DESCRIPTORS)

### A. Записи в small (0x0000–0x1FFF)

| Имя | Адрес | Размер | Назначение |
|---|---|---|---|
| `cfg_C660` | 0x0130 | 0x30 | Параметры конфигурации (версия?) |
| `cfg_C6F0` | 0x023C | 0x14 | Дополнительная конфигурация |
| `cfg_C690` | 0x0160 | 0x28 | Сетевые параметры |
| `cfg_BB7C` | 0x0188 | 0x20 | Параметры времени |
| `cfg_BBA0` | 0x0250 | 0x10 | Скорость/протокол |
| `cfg_B5B4` | 0x0260 | 0x80 | Расширенная конфигурация |
| `cfg_AB84` | 0x0478 | 0x50 | Калибровка |
| `cfg_AA84` | 0x04C8 | 0x80 | Настройки дисплея |
| `cfg_AB04` | 0x05A0 | 0x80 | Резервные параметры |

### B. Записи в large (SPI 0x2000+)

| Имя | Адрес | Размер | Назначение |
|---|---|---|---|
| `cfg_BD90` | 0x0EB8 | 0xE8 | Параметры 1 (с резервой) |
| `cfg_BE78` | 0x0DD0 | 0xE8 | Параметры 2 (с резервой) |
| `cfg_C26C` | 0x0FA0 | 0xE8 | Параметры 3 (с резервой) |
| `cfg_C354` | 0x1088 | 0xE8 | Параметры 4 (с резервой) |
| `cfg_C43C` | 0x1170 | 0xE8 | Параметры 5 (с резервой) |
| `cfg_AD0C` | 0x3928 | 0x110 | Расширенные параметры |

Каждая запись имеет основную копию и резервную копию (адрес = primary + смещение)

## VIII. Временные счётчики (Time Counters)

### A. Два блока в small

```
Block 0:
  ├─ Адрес: 0x0EE0
  ├─ Размер: 0x44 (68 байт)
  └─ Содержание: 8 пар (timestamp + uint32_counter)
     ├─ Пара 0: от 2000-01-01 + счётчик 0
     ├─ ...
     └─ Пара 7: от 2000-01-01 + счётчик 7

Block 1:
  ├─ Адрес: 0x0EE0 + 0x44 = 0x0F24
  └─ Аналогично Block 0
```

### B. Назначение

- Отслеживание моментов переключения тарифа
- Синхронизация с архивными записями
- История перехода между тарифами T1-T8

## IX. CRC-32 MSB и CRC-16 LE

### A. CRC-32 (для архивов)

```python
# Прошивка 0x304CC
def crc32_msb(data, init=0xFFFFFFFF):
  crc = init & 0xFFFFFFFF
  for byte in data:
    crc ^= byte << 24
    for _ in range(8):
      if crc & 0x80000000:
        crc = ((crc << 1) ^ 0x04C11DB7) & 0xFFFFFFFF
      else:
        crc = (crc << 1) & 0xFFFFFFFF
  return crc

# Для записи: берём младшие 16 бит в LE порядке
crc16_stored = (crc32_msb(data) & 0xFFFF).to_bytes(2, 'little')
```

### B. Верификация

```python
def verify_record(record):
  stored = int.from_bytes(record[-2:], 'little')
  calculated = crc32_msb(record[:-2]) & 0xFFFF
  return stored == calculated
```

## X. Практический пример: синхронная запись энергии

### A. Входные данные
- T1 = 1234 кВт·ч
- T2 = 5678 кВт·ч
- T3 = 910 кВт·ч
- T4–T8 = 0
- Делитель K = 1, знаков = 0

### B. Процесс редактора

```
1. Вычислить raw значения: raw_i = T_i * K = T_i (так как K=1)
2. Создать энергетическую запись:
   ├─ cell[0] = 1234 + 5678 + 910 + 0 + ... = 7822 (сумма)
   ├─ cell[1] = 1234 (raw_T1)
   ├─ cell[2] = 5678 (raw_T2)
   ├─ cell[3] = 910 (raw_T3)
   ├─ cell[4..8] = 0 (T4..T8 не используются)
   ├─ cell[9..12] = 0 (резерв)
   ├─ маркер = 0 (или текущий маркер поколения)
   └─ вычислить CRC-16 LE
3. Обновить:
   ├─ PRIMARY (0x0020)
   ├─ BACKUP (0x1A00)
   └─ ВСЕ 198 архивных слотов (type 0/1/2/5)
4. Записать в SPI через аппаратный интерфейс:
   ├─ разблокировка (ключ 0x5AA55AA5)
   ├─ запись страниц (256 байт за раз)
   ├─ блокировка (ключ 0xA55AA55A)
```

### C. Результат в SPI

```
Адрес 0x0020:
  00 04 00 00 00  # cell[0] = 0x0404 = 1028 (5 байт LE)
  d2 04 00 00 00  # cell[1] = 0x04d2 = 1234
  4e 16 00 00 00  # cell[2] = 0x164e = 5678
  86 03 00 00 00  # cell[3] = 0x0386 = 910
  00 00 00 00 00  # cell[4] = 0
  ... (5 нулевых ячеек)
  00              # маркер
  <CRC-16 LE>     # 2 байта контрольной суммы
```

## XI. Связь R1/W1 и SPI

| Протокол | Адрес | SPI primary | SPI backup | Тип | Доступ |
|---|---|---|---|---|---|
| R1/W1 0x0040 | RTC | 0x0000 | 0x19E0 | BCD (10б) | R: всегда, W: P2 |
| R1/W1 0x0000–0x0003 | Энергия Tn | 0x0020 | 0x1A00 | u40le (68б) | R: всегда, W: запрещена |
| R1/W1 0x0060 | Текущий тариф | 0x000A | 0x19EA | U8 (22б) | R: всегда, W: P2 |
| (нет прямого R1) | Архивы | 0x03A38–0x15097 | — | тип 0/1/2/5 | Синхро через W1 0x0020 |
| (нет прямого R1) | События | 0x47E70–0x49DEF | — | time+value+CRC | Буферизированы |

---

**Ключевой вывод**: R1/W1 команды протокола работают с логическими параметрами, которые физически хранятся в SPI с двойным резервированием и CRC защитой. Редактор программно воспроизводит ту же логику контрольных сумм и архивирования, что и прошивка.
