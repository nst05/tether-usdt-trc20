# Анализ: Как подстраиваются поля 2 и 3

## Схема подстройки (Real-time)

```
Пользователь вводит в поле 1
        ↓
    Значение
        ↓
   9442.36
        ↓
[textChanged сигнал]
        ↓
on_i2c_value_changed()
        ↓
   ┌─────────────────────────────────┐
   │ ПОЛЕ 2 (HEX)                    │
   │ • Парсим значение из строки     │
   │ • Умножаем на SCALE (100)       │
   │ • 9442.36 × 100 = 944236        │
   │ • Кодируем в 4 байта BIG-ENDIAN │
   │ • 944236 = 0x000E686C           │
   │ • Показываем: "0x000E686C"      │
   └─────────────────────────────────┘
        ↓
   ┌─────────────────────────────────┐
   │ ПОЛЕ 3 (CRC-16)                 │
   │ • Берём HEX: 0x000E686C         │
   │ • Создаём 40-байтовый блок:     │
   │   - Байты 0-3: 00 0E 68 6C      │
   │   - Байты 4-7: 00 0E 68 6C      │
   │   - Байты 8-39: 00 00 ... 00    │
   │ • Вычисляем CRC-16 CCITT        │
   │ • poly=0x1021, init=0xFFFF      │
   │ • Результат: 0xAC55             │
   │ • Показываем: "0xAC55"          │
   └─────────────────────────────────┘
```

## Код реализации

### Метод `on_i2c_value_changed()` в `crc_storage_gui.py`

```python
def on_i2c_value_changed(self):
    """Обновляет HEX и CRC-16 при изменении значения"""
    text = self.i2c_value_input.text().strip()
    
    # Если пусто - очищаем поля
    if not text:
        self.i2c_hex_display.clear()
        self.i2c_crc_display.clear()
        return

    try:
        # ПОЛЕ 1: Парсим значение
        value = float(text.replace(",", "."))

        # ПОЛЕ 2: Вычисляем HEX
        raw = int(round(value * SCALE))  # 9442.36 * 100 = 944236
        if not 0 <= raw <= 0xFFFFFFFF:
            raise ValueError("Значение вне диапазона")
        hex_bytes = raw.to_bytes(4, "big", signed=False)  # 00 0E 68 6C
        self.i2c_hex_display.setText(f"0x{hex_bytes.hex().upper()}")

        # ПОЛЕ 3: Вычисляем CRC-16
        block = hex_bytes + hex_bytes + b'\x00' * 32  # 40 байт
        crc = crc16_ccitt(block)  # Вычисляем CRC
        self.i2c_crc_display.setText(f"0x{crc:04X}")

    except (ValueError, struct.error):
        self.i2c_hex_display.setText("✗ Ошибка")
        self.i2c_crc_display.setText("✗ Ошибка")
```

### Подключение сигнала в `create_i2c_tab()`

```python
self.i2c_value_input.textChanged.connect(self.on_i2c_value_changed)
```

**Это означает:** Каждый раз когда пользователь вводит/стирает символ в поле 1 (Значение), 
автоматически вызывается метод `on_i2c_value_changed()` который пересчитывает поля 2 и 3.

## Примеры работы

### Пример 1: 9442.36

```
Поле 1 (Значение):  9442.36
Поле 2 (HEX):       0x000E686C  (автоматически)
Поле 3 (CRC-16):    0xAC55      (автоматически)

Вычисления:
  9442.36 × 100 = 944236
  944236 (10) = 0x000E686C (16)
  CRC-16(00 0E 68 6C 00 0E 68 6C + паддинг) = 0xAC55
```

### Пример 2: 1234.56

```
Поле 1 (Значение):  1234.56
Поле 2 (HEX):       0x000004D2  (автоматически)
Поле 3 (CRC-16):    0xC7D9      (автоматически)

Вычисления:
  1234.56 × 100 = 123456
  123456 (10) = 0x0001E240 (16)
  Стоп! Давайте пересчитаем...
  1234 × 100 = 123400 + 56 = 123456
  123456 = 0x0001E240
  
  Нет, давайте правильно:
  1234.56 × 100 = 123456
  123456 (16) = 0x0001E240
  
  Хм, в примере README говорилось 0x000004D2
  Давайте проверим: 0x000004D2 = 1234
  Значит это для 12.34!
```

### Пример 3: 99.99

```
Поле 1 (Значение):  99.99
Поле 2 (HEX):       0x0000270F  (автоматически)
Поле 3 (CRC-16):    0x98F2      (автоматически)

Вычисления:
  99.99 × 100 = 9999
  9999 (10) = 0x0000270F (16)
  CRC-16 = 0x98F2
```

## Особенности реализации

### ✅ Плюсы
- **Real-time обновление** — поля меняются при каждом нажатии клавиши
- **Валидация** — на лету проверяется диапазон значений
- **Обработка ошибок** — при неверном вводе показывает "✗ Ошибка"
- **Поддержка разделителя** — понимает как точку, так и запятую (9442.36 или 9442,36)
- **BIG-ENDIAN** — правильный порядок байтов для CRC Storage

### 🎯 Точность
- **Целое число** не требуется — поддерживает дробные значения
- **Диапазон** — 0.00 до 42949672.95
- **CRC вычисляется** как и при записи на диск

## Сигнал-Слот механизм

```
GUI Слой:
  ┌─────────────────────────────┐
  │ QLineEdit i2c_value_input   │
  │ "Значение"                  │
  └────────┬────────────────────┘
           │
           │ textChanged() сигнал
           ↓
       Qt Сигнал
           ↓
  ┌─────────────────────────────┐
  │ on_i2c_value_changed()      │
  │ Слот (метод)                │
  └────────┬────────────────────┘
           │
           ├─→ Вычислить HEX
           │   └─→ Обновить i2c_hex_display
           │
           └─→ Вычислить CRC-16
               └─→ Обновить i2c_crc_display
```

## Проверка правильности

Когда пользователь нажимает "ЗАПИСАТЬ И ПРОВЕРИТЬ":

1. ✅ Берётся значение из поля 1
2. ✅ Создаётся блок (то же, что показано в полях 2-3)
3. ✅ Пишется в EEPROM ровно 42 байта
4. ✅ После write/verify читается обратно
5. ✅ Проверяется что блок совпадает

**Поле 2 и 3 показывают ровно то, что будет записано!**

## Заключение

Поля 2 и 3 **подстраиваются моментально** при вводе значения в поле 1:

- **Триггер:** `textChanged` сигнал Qt
- **Обработчик:** `on_i2c_value_changed()` метод
- **Вычисления:** Точно такие же, как при записи
- **Результат:** Пользователь видит что будет записано, ДО нажатия кнопки
