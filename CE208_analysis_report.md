# Анализ прошивки CE208 (0b440eba-208_CE2_V8530P_full_512K.bin)

## 1. Идентификация
| Параметр | Значение |
|---|---|
| Устройство | Электросчётчик **СЕ208 (Энергомера)**, однофазный многотарифный |
| Строки в образе | `CE208 5.1,`  `CE208_2`  `CE2086.10` |
| Версия ПО / сборка | **`ver 10.14, Oct 29 2022`** (@ 0x72f9b) |
| Внутр. контр. сумма | `FCB9887C` (@ 0x72fb2, записана в образе) |
| Архитектура | **ARM Cortex-M0/M0+** (Thumb-1; нет MemManage/BusFault/UsageFault) |
| Размер образа | 524 288 байт (512 КБ) |
| CRC32 образа | `FC93178C` · MD5 `5374a2bd…` · SHA1 `b8cd5b58…` |

## 2. Карта памяти
| Регион | Адрес | Назначение |
|---|---|---|
| Flash (код) | `0x00000000` | вектора + код + данные |
| — вектора | `0x000000–0x0000BF` | 16 системных + 32 IRQ (48 слов) |
| — код/данные | `0x0000C0–0x073FFF` | ~464 КБ занятой прошивки |
| — блок версии | `0x072F90–0x072FD8` | строки идентификации |
| — стёртая флешь | `0x074000–0x07FFFF` | 48 КБ `0xFF` (резерв/данные) |
| SRAM | `0x20000000` | ОЗУ, использовано до `0x2000FFFB` → **≈64 КБ** |
| Начальный SP | `0x20000598` | вершина стартового стека |
| Периферия | `0x40000000`, `0x58000000`, `0x5C000000` | регистры MCU/АЦП |
| Ядро (PPB) | `0xE0000000` | NVIC / SysTick / SCB |

## 3. Таблица векторов (0x0000–0x00BF)
- **SP** = `0x20000598`, **Reset** = `0x0000C0`
- **NMI** `0x14FE`, **HardFault** `0x1510`, **SVCall** `0x1528`, **PendSV** `0x1540`, **SysTick** `0x1558`
- Внешние прерывания: задействованы **IRQ0…IRQ28** (обработчики `0x156E…0x17CC`), пропуски на IRQ25, 26, 29–31.
- Отсутствие обработчиков 4/5/6/12 подтверждает ядро **Cortex-M0/M0+**.

## 4. Последовательность запуска (Reset @ 0x00C0)
```
Reset:  blx  fn_0x00D8   ; инициализация/тест регистровых банков с верификацией (read-back, сравнение младших байт)
        blx  fn_0x288C   ; проверка целостности конфигурации + загрузка таблицы записей
        bx   fn_0x2AAC   ; обёртка main -> bl 0x2A4C (основной код)
```
- **fn_0x00D8** — цикл копирования слов с обратным чтением и сравнением (`ands rX,#0xFF; cmp`), выполняется для блоков по 0x20 и 0x30 слов. Инициализация/самотест аппаратных банков.
- **fn_0x288C** — читает счётчик записей `N` и его инверсию `~N`, сверяет две копии; при повреждении обеих — **вечный цикл `b .` (halt)**. Затем перебирает `N` записей по **12 байт** (`idx*3*4`), вызывая `fn_0x11DC` (загрузка/инициализация параметров из NVM).

## 5. Характер кода
- Обычный компилированный C (GCC-подобный), стандартные прологи `push {…,lr}` / эпилоги `pop {…,pc}`.
- **1535** прологов `push{lr}`, **2125** различных целей вызова, **10624** инструкции `BL/BLX`, **1269** подтверждённых функций.
- Идентифицированные примитивы:
  - `0x04FB24` — сборка 32-бит слова из 4 байт (`read_u32_le`) + диспетчер `switch` (`add pc, rX` по таблице) — **500 вызовов** (самый горячий).
  - `0x149F4` — байтовый `memcpy(dst,src,n)` — 387 вызовов.
  - `0xE8F4` — тик/счётчик состояния над структурой (инкремент 16-битных полей, сравнение с лимитами) — обработчик таймера/событий.
  - `0x11C44` — обход/обновление кольцевого счётчика записей.

## 6. Функциональные признаки (из строк)
- Роли доступа: **`ADMIN`**, **`Operator`** (уровни пароля меню).
- Форматирование даты/времени: `%02u.%02u.%02u`, `%02u-%02u %2u`, `%2u%02u%02u.%2u.%02u.%02u` — тарифный календарь/часы реального времени.
- Информ-строки: `InFo %u`, `%7s`, `%u`, таблица `abcdefghijklmnop` (hex/charset ЖКИ).

## 7. Инструменты и метод
- Дизассемблирование: сырой bin обёрнут в ELF (`EM_ARM`, база `0x0`), декодирование `llvm-objdump --triple=thumbv6m-none-eabi`.
- Полный линейный дизассемблинг кодовой области: `disasm_m0.txt` (0x00C0–0x74000, ~222k строк).
- Карта функций/xref: `function_map.txt`.

> Примечание: в линейном дизассемблинге data-области (таблицы, литеральные пулы, jump-таблицы) декодируются как «инструкции»/`<unknown>` — это ожидаемо для сырого образа без разметки секций.
