# CRC Storage Editor — CRC-16 CCITT

Набор утилит для работы с бинарными файлами, содержащими значения с контрольными суммами CRC-16 CCITT.

## Алгоритм

### CRC-16 CCITT
```
Полином:        0x1021
Инициализация:  0xFFFF
Отражение вх:   НЕТ
Отражение вых:  НЕТ
Final XOR:      0x0000
```

### Представление значений
- **Формат:** Fixed-Point (значение × 100)
- **Порядок:** Big-Endian (старший байт первым)
- **Размер:** 4 байта (32-бит)
- **Диапазон:** 0.00 – 42949672.95

### Структура блока (40 байт)
```
Байты 0–3:    Значение (4 байта, big-endian)
Байты 4–7:    Дубль значения (резервная копия)
Байты 8–39:   Паддинг нулями (32 байта)
```

### CRC хранение
```
Позиция:   После 40-байтового блока
Размер:    2 байта (16-бит, big-endian)
Вычисл.:   Для полного 40-байтового блока
```

## Примеры

| Значение | Целое число | HEX        | CRC-16 |
|----------|-------------|------------|--------|
| 9442.36  | 944236      | 0x000E686C | 0xAC55 |
| 9440.38  | 944038      | 0x000E67A6 | 0x8748 |
| 12.34    | 1234        | 0x000004D2 | 0xC7D9 |
| 50.00    | 5000        | 0x00001388 | 0x3798 |
| 99.99    | 9999        | 0x0000270F | 0x98F2 |

## Установка

### Требования
- Python 3.6+
- PyQt5

```bash
# Установка зависимостей
pip install PyQt5
```

### Сборка в exe (Windows)
```bash
pip install pyinstaller

# GUI версия
pyinstaller --onefile --windowed crc_storage_gui.py

# CLI версия
pyinstaller --onefile crc_storage_cli.py
```

## Использование

### GUI приложение (рекомендуется)

```bash
python crc_storage_gui.py
```

**Возможности:**
- Открытие/загрузка бинарных файлов
- Просмотр всех значений и их контрольных сумм
- Редактирование значений с автоматическим пересчётом CRC
- Сохранение файлов с резервной копией
- Валидация всех значений

### CLI утилита

#### Показать все значения
```bash
python crc_storage_cli.py list file.bin
```

**Вывод:**
```
📄 Файл: file.bin
   Размер: 2048 байт (2.0 КБ)

Позиция      Значение    CRC-16   Статус
0x0001C1     9442.36     0xAC55   ✓ OK
0x000201     9440.38     0x8748   ✓ OK

Всего: 2 значений
```

#### Прочитать значение
```bash
python crc_storage_cli.py get file.bin 0x01C1
```

**Вывод:**
```
✓ Позиция 0x0001C1: 9442.36
```

#### Записать значение
```bash
python crc_storage_cli.py set file.bin 0x01C1 9999.99
```

**Вывод:**
```
✓ Позиция 0x0001C1:
  Старое значение: 9442.36
  Новое значение: 9999.99
  Новый CRC-16: 0xXXXX
✓ Файл сохранён
  Резервная копия: file.bak
```

#### Справка об алгоритме
```bash
python crc_storage_cli.py info
```

## Примеры использования

### Python скрипт
```python
from crc_storage_cli import StorageFile, make_block, crc16_ccitt
import struct

# Загружаем файл
storage = StorageFile('data.bin')
storage.load()

# Показываем все значения
for item in storage.values:
    print(f"0x{item['pos']:06X}: {item['value']:.2f} (CRC: 0x{item['crc']:04X})")

# Обновляем значение
storage.write_value(0x01C1, 1234.56)

# Сохраняем
storage.save()
```

### Вычисление CRC для значения
```python
from crc_storage_cli import encode_value, crc16_ccitt

value = 9442.36
val_bytes = encode_value(value)
block = val_bytes + val_bytes + b'\x00' * 32
crc = crc16_ccitt(block)

print(f"Значение: {value}")
print(f"HEX: 0x{int(value*100):08X}")
print(f"CRC-16 CCITT: 0x{crc:04X}")
```

## Структура файла в шеснадцатеричной системе

Пример для значения 9442.36 на позиции 0x01C1:

```
Позиция  Содержимое
────────────────────────────────────────────────────────────────
0x01C1   00 0E 68 6C          ← Значение (9442.36 × 100 = 944236)
0x01C5   00 0E 68 6C          ← Дубль (резервная копия)
0x01C9   00 00 00 00          ← Нули (паддинг начало)
0x01CD   00 00 00 00
...
0x01E5   00 00 00 00
0x01E9   AC 55                ← CRC-16 CCITT (0xAC55)
0x01EB   FF FF FF FF          ← Остальные данные файла
```

## Безопасность

- ✓ Автоматическое создание резервной копии при сохранении
- ✓ Валидация CRC для всех найденных значений
- ✓ Не трогает остальные данные в файле
- ✓ Поддерживает разные позиции и структуры в одном файле

## Ограничения

- Максимальное значение: 42949672.95
- Минимальное значение: 0.00
- Поддерживаются только 2-байтовые CRC-16
- Значения хранятся в big-endian формате

## Лицензия

MIT License

## Автор

Разработано для работы с CRC-16 CCITT хранилищем значений.

## Поддержка

Для сообщения об ошибках или предложений создайте issue в репозитории.
