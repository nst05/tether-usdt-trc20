# СЕ208 — команды оптопорта (реверс прошивки ver 10.14)

## Физический уровень
Оптопорт — один из 3 последовательных каналов (ISR `0x39088`, объект канала 0x240 б).
Оптический интерфейс по **ГОСТ Р МЭК 61107** (IEC 61107), протокол обмена —
**IEC 62056-21, режим C** (программирование/чтение). Найдено в прошивке напрямую:
шаблоны кадров @0x6B2C0, строка запроса `/?!` @0x326C8, идентификатор `CE208…` @0x72FAC.

Управляющие байты: `SOH=0x01`, `STX=0x02`, `ETX=0x03`, `ACK=0x06`, `CR LF=0x0D 0x0A`.
Кадр команды: `SOH <мнемоника> STX <данные> ETX <BCC>`, где BCC — XOR-контроль от байта
после SOH до ETX включительно.

## Сеанс (mode C handshake)
1. Мастер → `/?!CRLF`  (или `/?<адрес>!CRLF` для адресной посылки).  [`/?` `/?!` @0x326C0]
2. Счётчик → `/CE208…CRLF` — идентификация (строка «CE208 5.1» / версия «ver 10.14»).
3. Мастер → `ACK 0 Z 1 CRLF` — выбор скорости (Z — код бод), переход в режим программирования.
4. Счётчик → `SOH P0 STX (ident)ETX BCC` — открытие сеанса.
5. Мастер → пароль, далее команды чтения/записи. Конец сеанса — `SOH B0 ETX BCC` (break).

## Таблица команд (шаблоны кадров @0x6B2C0)
| Мнемоника | Кадр (hex) | Назначение |
|---|---|---|
| **P0** | `01 50 30 02 28 25 6C 58 29 03` = `SOH P0 STX (%lX) ETX` | открытие сеанса / идентификатор (hex) |
| **P1** | `01 50 31 02 28` = `SOH P1 STX (` | ввод **пароля** (уровень доступа 1) |
| **P2** | `01 50 32 02 28` = `SOH P2 STX (` | второй уровень доступа / разрешение записи |
| **R1** | `01 52 31 02` = `SOH R1 STX` | **чтение** регистра: `R1 STX <адрес>() ETX BCC` |
| **W1** | `01 57 31 02` / `01 57 31 28` = `SOH W1 STX`/`(` | **запись** регистра: `W1 STX <адрес>(<данные>) ETX BCC` |

Строки-форматтеры данных из той же таблицы: `L(%X,%X,%X%c)` (строка данных),
`(%lX)` (hex-значение), `,%.3f` (числовое значение с 3 знаками).

Подтверждение по сравнениям в коде (кол-во `cmp #imm`):
`/`(0x2F)=7, `!`(0x21)=10, `ACK`(0x06)=111, `0`=120, `1`=16, `2`=23, `P`(0x50)=2, `R`(0x52)=3.

## Разбор адреса регистра (R1/W1)
Функция `0x326D2` — поиск запрашиваемого адреса в таблице параметров:
- маска ширины адреса выбирается по типу канала (r1: 1/4 → таблица −0x1F; 2 → маска 0xFE00);
- бинарный/линейный проход по таблице параметров (`0x32800`, `0x32804`), сравнение
  маскированного адреса `r6`;
- при совпадении вызывает обработчик параметра, при промахе → ответ ошибки (`0x20`).

Адреса соответствуют регистрам счётчика (энергия по тарифам, напряжение/ток, дата/время,
конфигурация). Их дерево описано дескрипторами @0x737xx и таблицами аксессоров @0x5D9xx.

## Уровни доступа
- Чтение (R1) большинства регистров — без пароля.
- Запись (W1) и параметрирование — только после `P1`/`P2` с корректным паролем
  (проверка пароля — отдельная ветка; сам пароль хранится во внутренней NVM, не в коде).

> Примечание: это документация протокола по результатам реверса. Чтение показаний по
> оптопорту — штатная функция; запись параметров защищена паролем на стороне прибора.
