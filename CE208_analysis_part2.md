# CE208 — глубокий разбор (Часть 2): ядро RTOS, прерывания, связь

## A. Подтверждение архитектуры
- Скан всего кода (0x0–0x74000): кандидатов `MOVW`=3, `MOVT`=2, `TBB/TBH`=0 при 10922 `BL`.
  Настоящий Cortex-M3 содержал бы сотни `movw/movt` — их нет.
- Вывод: **ARM Cortex-M0 / M0+, только подмножество Thumb-1**. Отсутствие обработчиков
  MemManage/BusFault/UsageFault согласуется.
- Обращения к региону `0x42000000` (напр. `str` по 0x42000918, 0x4228037C) — это **не** bit-band
  (у M0 его нет), а вендор-специфичная периферия данного MCU.
- Уточнение по карте флеши: верхняя область `0x74000–0x80000` не полностью пустая —
  содержит таблицы конфигурации/калибровки (напр. по 0x7E0C4 — пары «регистр/маска»,
  по 0x7FC00 — заводские данные).

## B. Примитивы ядра (RTOS)
| Адрес | Роль | Как работает |
|---|---|---|
| `0x029E` | guard входа в ISR | `ldr r0,=flag; ldrb r0,[r0]` — возвращает признак «планировщик занят/не готов» |
| `0x1898` | **диспетчер события** | `ldr r0,[r0]; blx r0` — вызывает `desc->handler()` из 1-го слова дескриптора |
| `0x18A2` | exit critical | `movs r0,#0; msr primask,r0` (разрешить прерывания) |
| `0x18AA` | enter critical | `movs r0,#1; msr primask,r0` (запретить прерывания) |
| `0x14EC` | фатальный обработчик | `dsb; str <v>,[reg]; dsb; b .` — фиксация отказа + вечный цикл (перезапуск по WDT) |

Модель: аппаратный вектор → `0x29E` (guard) → `0x1898(desc)` → вызов зарегистрированного
C-обработчика. Дескрипторы — константы во флеши: system @ `0x4008…0x403C`,
внешние IRQ — таблица `@0x4040 + IRQ*4`.

## C. Полная карта прерываний (реальные обработчики)
| Вектор | Дескриптор | C-обработчик | Что делает |
|---|---|---|---|
| HardFault | 0x4008 | 0x70E0E → 0x70F48 | ставит бит отказа (obj+0x11 \|=1) |
| NMI | 0x400C | 0x70E16 → 0x70F60 | обработка + halt |
| SVC/PendSV/SysTick | 0x402C/38/3C | 0x70E1E/26/2E → 0x70DFC | планировщик (переключение задач/тик) |
| **IRQ0** | 0x4040 | **0x70E36** | многоканальный контроллер (DMA/EXTI): опрос+сброс битов 0x1/0x4/0x20/0x40/0x200 через `0x4DDF6`(pending)/`0x4DE00`(clear), для каждого — флаг/суб-обработчик |
| **IRQ1** | 0x4044 | 0x70EBC | источник №4 → `0x4F180` + post `0x70FA2` |
| IRQ5/6/7 | 0x4054/58/5C | 0x70ED2/DC/E6 | **общий драйвер связи** `0x39088(0/1/2)` — 3 канала |
| IRQ11 | 0x406C | 0x70EF4 | `0x56BB4` |
| **IRQ13** | 0x4074 | обёртка 0x16BE | **аппаратная периферия @0x40012800**: `0xF3E`(чтение/сброс), `obj+0xC=1`, post `0x1AF0` |
| IRQ14 | 0x4078 | 0x70F00 | периферия с флагом завершения (obj+0xC=1) |
| IRQ2,3,4,9,10,12,15–24,27,28 | — | `bx lr` | заглушки (не используются) |

## D. Подсистема связи (3 канала)
Общий ISR **`0x39088(chan)`**, `chan∈{0,1,2}`:
- Объект канала: `base=0x20009264` (SRAM), шаг **0x240 байт** (`chan*9*64`).
- Программные флаги события (в RAM-структуре, не в HW-регистре), тест через `0x4BE48(obj,mask)`:
  - `0x02|0x08` → **TX**: `obj+4=8`, `0x399FC(chan)`; затем `obj+0xC=2`, `0x399DC(chan)`.
  - `0x20` → **RX**: таблица байт-флагов `0x2000BD18+chan`, `0x39A10(chan)`,
    режимный switch по chan → запись флагов, `0x393E0(chan,0)`, `0x39A18(chan)`.
  - иначе → **ошибка**: `obj+4=0xBC`, `obj+0xC=0x3E`.
- Три канала соответствуют физическим портам счётчика: **оптопорт + интерфейсный (RS-485/CAN/PLC) + служебный**.

Слоистость: HW-ISR (клиринг флага периферии) → post события в планировщик →
задача обслуживания канала → парсер кадров протокола (Энергомера/СЕ).

## E. Что осталось «под капотом» (следующий шаг)
- Парсер кадров протокола обмена (адрес/команда/CRC) — точка входа со стороны `0x39A10`(RX-байт)
  и `0x393E0` (обработка режима). Требует отдельного трассирования конечного автомата приёма.
- Драйвер ЖКИ и раскладка сегментов (таблица `abcdefghijklmnop`, форматтеры даты/времени).
- Тарифный календарь и счётчики энергии (структуры на 12 байт из stage-2 инициализации).
