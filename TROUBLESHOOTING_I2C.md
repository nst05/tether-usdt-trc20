# Диагностика проблем I2C/EEPROM

## Если получаете ошибку "Проверка записи не пройдена (10 попыток)"

Теперь в приложении добавлено **автоматическое логирование** которое помогает найти проблему!

### Что показывают DEBUG ЛОГИ

Когда запись не удаётся, программа показывает детальные логи:

```
Распарсено значение: 1453.XX
Программатор открыт
Текущее значение: 9442.36 (байты: 000E686C)
Блок создан: 42 байт, CRC: 0x4361
Данные: 00 02 37 94 00 02 37 94 (первые 8 байт)
Записано по смещению 0x0000
Программатор закрыт
Ожидание завершено
Программатор переоткрыт
Попытка 1: Прочитано 9442.36, CRC: 0xAC55
  Байты: 000E686C 000E686C (первые 8 байт)
✗ Все 10 попыток исчерпаны, проверка не пройдена
Ожидали: 00023794
Прочитали: 000E686C
```

## Анализ логов

### ✅ Что означает каждая строка

| Логи | Статус | Что это означает |
|------|--------|-----------------|
| `Программатор открыт` | ✓ OK | I2C CH341 подключен и отвечает |
| `Текущее значение прочитано` | ✓ OK | Чтение из EEPROM работает |
| `Записано по смещению 0xXXXX` | ✓ OK | Запись команда отправлена |
| `Попытка X: Прочитано Y` | ? ПРОВЕРКА | Но значение старое, а не новое! |

### ❌ Проблемы и решения

#### **Проблема 1: Прочитали старое значение вместо нового**

```
Ожидали: 00023794
Прочитали: 000E686C
```

**Причины:**
1. **EEPROM не записалась** — может быть:
   - Контакт слаб
   - Время ожидания 0.15s недостаточно
   - Адресация неверна

2. **Решения:**
   - Проверьте физические контакты
   - Увеличьте время ожидания (строка в коде: `time.sleep(0.15)` → `time.sleep(0.5)`)
   - Проверьте адрес EEPROM и адрес устройства I2C

#### **Проблема 2: Ошибка при открытии программатора**

```
ИСКЛЮЧЕНИЕ: Программатор не инициализирован
```

**Причины:**
1. CH341 не подключен
2. Драйверы не установлены
3. i2cpy не видит устройство

**Решения:**
```bash
# Проверьте подключение
lsusb | grep "QinHeng"

# Переустановите драйверы
pip install --upgrade i2cpy
```

#### **Проблема 3: Ошибка при чтении**

```
Попытка 1: Ошибка чтения - Адрес выходит за пределы...
Попытка 2: Ошибка чтения - ...
```

**Причины:**
1. Смещение адреса (OFFSET) неверно
2. Размер EEPROM мал
3. Проблема с I2C шиной

**Решения:**
- Проверьте значение `Смещение в EEPROM (опционально): 0x0000`
- Должно быть в диапазоне 0x0000-0x07FF для 24C256
- Проверьте что блок не выходит за пределы EEPROM

## Как исправить таймаут записи

Если EEPROM медленная, увеличьте ожидание:

**В crc_storage_i2c.py строка ~230:**

```python
# Текущее значение (может быть слишком мало):
time.sleep(0.15)

# Попробуйте увеличить:
time.sleep(0.25)  # или даже 0.5
```

## Проверка вручную

Если хотите проверить EEPROM напрямую без GUI:

```python
from crc_storage_i2c import I2CWriter

writer = I2CWriter(offset=0x0000)

# Опция 1: Без debug
result = writer.write_and_verify("1453")
print(result)

# Опция 2: С debug (видны все шаги)
result = writer.write_and_verify("1453", debug=True)
print(result['debug'])
```

## Чек-лист

Если запись не работает:

- [ ] CH341 программатор видим в `lsusb`?
- [ ] EEPROM 24C256 подключена правильно?
- [ ] Контакты I2C (SDA, SCL) припаяны?
- [ ] Питание 5V подано на EEPROM?
- [ ] Адрес EEPROM правильный (0x50-0x57)?
- [ ] Смещение в диапазоне 0x0000-0x07FF?
- [ ] Значение вводите корректное (не более 42949672.95)?

## Логирование в файл

Если хотите сохранить логи для анализа:

```python
result = writer.write_and_verify("1453", debug=True)

with open("debug.log", "w") as f:
    f.write(result['debug'])
```

## Контакты EEPROM 24C256

```
     ┌──────┐
  1  │      │  8  (VCC)
  2  │      │  7  (NC или WP)
  3  │      │  6  (SCL)
  4  │      │  5  (SDA)
     └──────┘
```

- Pin 1,2,3: GND (земля)
- Pin 4: GND (земля)
- Pin 5: SDA (I2C данные)
- Pin 6: SCL (I2C часы)
- Pin 7: Write Protect (опционально, обычно в GND)
- Pin 8: VCC (5V питание)

## Если всё равно не работает

1. **Сохраните debug логи** из приложения
2. **Проверьте:**
   - Напряжение на EEPROM (должно быть 5V)
   - Сопротивления pull-up на SDA/SCL (обычно 4.7k)
   - Нет ли коротких замыканий

3. **Попробуйте другой адрес** (по умолчанию 0x0000):
   - Откройте "Смещение в EEPROM (опционально)"
   - Введите `0x0100` или `0x0200`

Если проблема в EEPROM, может быть повреждена и нужна замена.
